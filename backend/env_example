NODE_ENV=production

PORT=3333

DATABASE_USERNAME=root
DATABASE_PASSWORD=secret
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=mydb
DATABASE_URL=postgres://${DATABASE_USERNAME}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}_${NODE_ENV}